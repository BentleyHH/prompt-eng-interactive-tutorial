PyMuPDF>=1.24      # PDF lesen/overlay/ersetzen (Modul: fitz)
reportlab>=4.0     # PDF neu rendern (generate-Vorlage)
qrcode>=7.4        # QR-Code-Erzeugung
python-barcode>=0.15  # Code128-Barcodes (CBRN-Exhibit-Booklet)
zxing-cpp>=2.2,<3  # QR/Barcode dekodieren (Verifikation) – selbst-enthalten, KEINE Systembibliothek.
                   # <3, weil 3.0 für Python 3.9 (macOS) kein Fertigpaket hat und sonst kompiliert würde.
Pillow>=10.0       # Bildverarbeitung (QR/Barcode-Bild, Logo)
openpyxl>=3.1      # Excel (.xlsx) lesen/schreiben
Flask>=3.0         # lokales Web-Dashboard (webapp/), inkl. Werkzeug (Login/Passwort-Hash)

# Optionaler Decoder-Fallback (nur falls zxing-cpp nicht läuft): pyzbar (+ libzbar).
# Optional für PDF/A-Archivkopien (Systemwerkzeug):
#   macOS: brew install ghostscript   |   Debian/Ubuntu: sudo apt-get install -y ghostscript
